import { {bigName} } from './{underTest}{name}';

describe('{bigName}', () => {
  it('should create an instance', () => {
    const directive = new {bigName}({params});
    expect(directive).toBeTruthy();
  });
});
